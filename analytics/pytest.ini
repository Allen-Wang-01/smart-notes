[pytest]
testpaths = test
# Show local variable values on failure — very helpful for debugging
addopts = -v --tb=short --showlocals
# Treat warnings as info (not errors) during tests
filterwarnings =
    ignore::DeprecationWarning